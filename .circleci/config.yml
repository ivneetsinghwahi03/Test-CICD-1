version: 2.1

workflows:
  test-deploy:
    jobs:
      - test
      - test1
        context:
            - ns-auth

jobs:
  test:
    docker:
      - image: suitehearts/node-sdf
    steps:
      - checkout
      - run: suitecloud --version

  test1:
    docker:
      - image: suitehearts/node-sdf
    steps:
      - checkout
      - run: echo $TOKEN_ID && $TOKEN_SECRETE
      
   


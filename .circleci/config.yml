version: 2.1

workflows:
  test-deploy:
    jobs:
      -test
      - test1

jobs:
  test:
    docker:
      - image: suitehearts/node-sdf
    steps:
      - checkout
      - run: suitecloud --version

  test1:
    docker:
      - image: suitehearts/node-sdf
    steps:
      - checkout
      - run: suitecloud --version

